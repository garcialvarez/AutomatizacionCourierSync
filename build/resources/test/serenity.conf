serenity {
    take.screenshots = FOR_FAILURES
    use.unique.browser = true
    test.root = co.edu.udea.certificacion.empresaEnvios
    console.colors = true
    timeout = 30000
    report {
        accessibility = true
        types = "epic, feature"
    }
}


webdriver {
  driver = chrome
  autodownload = true
  timeouts {
    implicitlywait = 30000
    fluentwait = 30000
  }
  wait.for.timeout = 30000
  capabilities {
    browserName = "chrome"
    acceptInsecureCerts = true
  }
}

chrome.switches = """--start-maximized;--test-type;--no-sandbox;--ignore-certificate-errors;
                   --disable-popup-blocking;--disable-default-apps;--disable-extensions-file-access-check;
                   --incognito;--disable-infobars,--disable-gpu;--remote-allow-origins=*"""

pages {
    swaglabsUrl = "https://www.google.com/"
}